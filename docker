docker run -d \
  --name=cups \
  --restart=unless-stopped \
  --net=host \
  --privileged \
  -v /var/run/dbus:/var/run/dbus \
  -v /opt/docker/cups/config:/config \
  -v /opt/docker/cups/services:/services \
  -v /dev/bus/usb:/dev/bus/usb \
  -e CUPSADMIN="admin" \
  -e CUPSPASSWORD="admin" \
  chuckcharlie/cups-avahi-airprint:latest

docker run -d \
  --name=cups \
  --restart=unless-stopped \
  --net=host \
  --privileged \
  -v /var/run/dbus:/var/run/dbus \
  -v /opt/docker/cups/config:/etc/cups \
  -v /dev/bus/usb:/dev/bus/usb \
  -e CUPSADMIN="admin" \
  -e CUPSPASSWORD="admin" \
  ydkn/cups:latest
